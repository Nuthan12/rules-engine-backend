server.port=8080
spring.application.name=rules-engine-backend

# --- MongoDB Configuration ---

# FOR PRODUCTION (MongoDB Atlas Cloud)
# 1. Get your connection string from the MongoDB Atlas dashboard (Connect -> Drivers).
# 2. Replace <username>, <password>, and <database-name> with your actual credentials.
# 3. IMPORTANT: For production, it is highly recommended to set this value as an
#    environment variable on your hosting platform (e.g., Render) instead of putting it here.
#    The variable name would be: SPRING_DATA_MONGODB_URI
# spring.data.mongodb.uri=mongodb+srv://your_user:your_password@your_cluster.mongodb.net/rulesdb?retryWrites=true&w=majority

# FOR LOCAL DEVELOPMENT (using the mongodb service in docker-compose.yml)
# Keep this line uncommented when you are running the application locally.
spring.data.mongodb.uri=mongodb+srv://ruleengine:Ruleengine123@personal.ktqvkuv.mongodb.net/rulesdb?retryWrites=true&w=majority&appName=Personal

# NLP Service configuration (points to the service name in docker-compose)
nlp.service.url=http://nlp-service:8000/parse-rule

# JWT Configuration
jwt.secret=ThisIsAReallyLongAndVerySecretKeyForOurRulesEngineApplicationThatShouldBeExternalizedAndBeAtLeast256BitsLongForHS256
jwt.expiration.ms=86400000
